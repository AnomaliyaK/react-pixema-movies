import styled from "styled-components";
import { COLOR } from "ui";

export const StyledSettingsPage = styled.div`
  display: grid;
  gap: 40px;
  margin-left: 306px;
  margin-top: 112px;
  color: ${COLOR.WHITE};
`;
