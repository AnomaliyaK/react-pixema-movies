import styled from "styled-components";
import { COLOR } from "ui";

export const StyledFooter = styled.footer`
  color: ${COLOR.LIGHT};
  font-size: 16px;
  font-weight: 500;
  /* align-items: end; */
`;
